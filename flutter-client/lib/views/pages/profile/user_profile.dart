import 'package:flutter/material.dart';

class UserProfileSceen extends StatelessWidget {
  const UserProfileSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("User Profile"),
    );
  }
}